using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExempleBase : MonoBehaviour
{
    string monNon = "Martine";
    
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(monNon);
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("Bye");
    }
}
